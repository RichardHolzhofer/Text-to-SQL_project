ecommerce_analytics:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "{{ env_var('LOADER_SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('LOADER_SNOWFLAKE_USER') }}"
      password: "{{ env_var('LOADER_SNOWFLAKE_PASSWORD') }}"
      role: "{{ env_var('LOADER_SNOWFLAKE_ROLE') }}"
      warehouse: "{{ env_var('LOADER_SNOWFLAKE_WAREHOUSE') }}"
      database: "{{ env_var('LOADER_SNOWFLAKE_DATABASE') }}"
      schema: "{{ env_var('LOADER_SNOWFLAKE_SCHEMA') }}"
      threads: 4
      client_session_keep_alive: False